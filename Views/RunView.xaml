<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Configer.Views.RunView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    MinWidth="600"
    MinHeight="400">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- <StackPanel Grid.Row="0" Orientation="Vertical" Spacing="4">
            <TextBlock Text="内嵌 PowerShell 终端，支持 Rich/ANSI 输出与 Tab 自动补全。"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel> -->

        <Grid Grid.Row="0" ColumnSpacing="12" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Spacing="4" Grid.Row="0" Grid.Column="0">
                <TextBlock Text="工作目录" FontWeight="SemiBold" />
                <TextBlock x:Name="WorkingDirectoryText" FontFamily="Consolas" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="1">
                <Button x:Name="RestartButton" Content="重新启动" Click="RestartButton_Click" Style="{StaticResource AccentButtonStyle}" />
                <Button x:Name="StopButton" Content="退出" Click="StopButton_Click" />
                <Button x:Name="CtrlCButton" Content="Ctrl+C" Click="CtrlCButton_Click" />
                <Button x:Name="ClearButton" Content="清屏" Click="ClearButton_Click" />
            </StackPanel>

            <winui:InfoBar x:Name="StatusInfoBar" Grid.Row="1" Grid.ColumnSpan="2" IsOpen="False" Margin="0,12,0,0" />
        </Grid>

        <Border Grid.Row="1" CornerRadius="8" Background="#0f172a" BorderBrush="#1f2937" BorderThickness="1" MinHeight="120">
            <winui:WebView2 x:Name="TerminalWebView" DefaultBackgroundColor="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinHeight="520" />
        </Border>
        
    </Grid>
</UserControl>
